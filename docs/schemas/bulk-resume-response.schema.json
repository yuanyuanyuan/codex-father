{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Bulk Resume Response (v1)",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "success": { "type": "boolean" },
    "data": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "dryRun": { "type": "boolean" },
        "preview": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "jobId": { "type": "string" },
              "state": { "type": ["string", "null"] },
              "resumeFrom": { "type": ["integer", "null"] },
              "eligible": { "type": "boolean" },
              "reason": { "type": ["string", "null"] }
            },
            "required": ["jobId", "eligible"]
          }
        },
        "resumed": { "type": "array", "items": { "type": "string" } },
        "failed": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "jobId": { "type": "string" },
              "error": { "type": "string" }
            },
            "required": ["jobId", "error"]
          }
        },
        "summary": {
          "type": "object",
          "properties": {
            "total": { "type": "integer" },
            "resumed": { "type": "integer" },
            "failed": { "type": "integer" }
          }
        },
        "resumeFrom": { "type": ["integer", "null"] },
        "skipCompleted": { "type": ["boolean", "null"] },
        "advice": {
          "type": "object",
          "properties": {
            "retry": {
              "type": "object",
              "properties": {
                "suggested": { "type": "string", "enum": ["none", "retry_failed_only"] },
                "cooldownSeconds": { "type": "integer", "minimum": 0 }
              },
              "required": ["suggested", "cooldownSeconds"]
            },
            "rollback": {
              "type": "object",
              "properties": {
                "supported": { "type": "boolean" },
                "mode": { "type": "string", "enum": ["none", "manual"] }
              },
              "required": ["supported", "mode"]
            }
          },
          "required": ["retry", "rollback"]
        }
      },
      "required": ["dryRun", "advice"]
    }
  },
  "required": ["success", "data"]
}

